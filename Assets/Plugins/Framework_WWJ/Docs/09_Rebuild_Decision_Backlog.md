# Framework_WWJ 重建设计待办

> 状态：等待用户补充阶段需求和第一个游戏目标。<br>
> 本文是决策入口，不是已经批准的阶段路线。

## 1. 已确认边界

- 从零设计 Framework_WWJ，不要求兼容旧框架或 HTY API。
- 框架采用模块化思维，主体负责模块装配、卸载和统一时序驱动。
- HTY / ActFramework 是主要参考目标，但只复用经过验证的问题定义与设计思想。
- 优先追求轻量、可理解、可测试，再随真实游戏目标逐步扩展。
- `Main` 与 `Utils` 的旧代码已经清理；正式设计确认前不恢复旧类型或建立并行 V2。

## 2. 进入基础骨架前的决策

| 主题 | 必须回答的问题 | 所需证据 | 状态 |
| --- | --- | --- | --- |
| 模块载体 | 普通 C#、ScriptableObject、MonoBehaviour 或组合？ | 配置工作流、运行状态、测试与实例所有权 | 待设计 |
| 配置分离 | 资产配置是否与运行时模块实例分开？ | Play Mode 资产污染、复用与克隆需求 | 待设计 |
| 作用域 | 是否需要 global、scene 或更多作用域？ | 首个游戏目标的场景数量和跨场景服务 | 待用户输入 |
| 生命周期 | 最少需要哪些阶段？是否异步？ | 真实加载任务、取消、失败和回滚场景 | 待用户输入 |
| 初始化顺序 | 使用优先级、显式依赖、拓扑排序或人工顺序？ | 模块依赖示例与循环依赖策略 | 待设计 |
| Tick | 默认方法还是显式能力接口？ | 每帧模块数量和非 Tick 模块比例 | 待设计 |
| 模块访问 | key、类型、接口、上下文或受控静态入口？ | 可测试性、重复实例和调用体验 | 待设计 |
| 重复规则 | 如何判断两个模块冲突？ | 作用域与多实例模块用例 | 待设计 |
| 错误策略 | 单模块失败后停止、跳过还是回滚？ | 游戏启动与场景切换的容错目标 | 待用户输入 |
| Handler | 强制抽象、可选组合还是不进入核心？ | 至少一个简单模块和一个复杂模块对比 | 待验证 |
| 程序集 | Core、Runtime、Editor、Tests 如何分界？ | 首批模块与第三方依赖 | 待设计 |
| 诊断 | 最少需要哪些日志、状态快照和验证工具？ | 测试与排错工作流 | 待设计 |

## 3. 基础模块候选待办

以下不是首批实现清单。每个模块都必须由游戏目标触发后单独定义契约：

- 日志与诊断
- 时间与 Tick
- 场景流程
- 资源访问
- 对象池
- 音频
- 输入
- UI
- 存档

为每个候选补齐：使用者、公开 API、所有权、依赖、同步/异步语义、失败行为、资源释放、EditMode/PlayMode 验收和明确非目标。

## 4. 阶段需求输入模板

用户提出下一阶段时，先记录以下内容；缺失且会改变架构的部分必须补齐后再实施。

```markdown
# 阶段名称

## 目标
- 这一阶段要解决的具体问题：
- 玩家或开发者可观察到的结果：

## 游戏场景
- 核心循环：
- 场景数量与切换方式：
- 目标平台：
- 涉及的资源和数据：

## 范围
- 必须实现：
- 明确不实现：
- 可接受的破坏性变化：

## 参考
- 希望借鉴的 HTY 机制：
- 希望避免的复杂度：
- 其他参考项目：

## 验收
- EditMode：
- PlayMode：
- 垂直切片：
- 性能或内存指标：
```

## 5. 架构决策记录模板

每项影响核心契约的决定都在 Docs 中留下 ADR：

```markdown
# ADR-XXX：标题

- 状态：提议 / 已接受 / 已替代
- 日期：
- 所属阶段：

## 背景与约束

## 候选方案

## 决定

## 影响与明确非目标

## 验证方式
```

一个 ADR 只解决一个主要选择。被替代时保留原文并链接新 ADR，避免历史原因再次丢失。

## 6. 单阶段门禁

### 设计就绪

- [ ] 用户目标、范围和非目标已记录
- [ ] 关键术语与所有权没有歧义
- [ ] 公共接口和数据流已描述
- [ ] 失败、取消和卸载行为已描述
- [ ] 验收测试能够证明阶段目标
- [ ] 与 HTY 的关系已标记为借鉴、简化或舍弃

### 实现完成

- [ ] 只实现本阶段批准范围
- [ ] Runtime、Editor、Tests 依赖方向正确
- [ ] Unity 编译或等价静态验证通过
- [ ] 约定的 EditMode / PlayMode / 垂直切片通过
- [ ] 文档、ADR 和源码索引已回写
- [ ] 记录真实摩擦点，作为下一阶段输入，不立即扩张核心

## 7. 下一步需要用户提供

1. 第一个可验证游戏目标；
2. 该目标必须使用的最少模块；
3. 场景与跨场景状态模型；
4. 对异步加载、取消和错误恢复的最低要求；
5. 希望优先借鉴或明确避免的 HTY 设计。

收到这些信息后，再形成正式的第一阶段设计与验收计划。
